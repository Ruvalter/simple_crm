<%= simple_form_for @company do |f| %>

  <% if @company.errors.any? %>
    <ul>
      <% @company.errors.full_messages.each do |error| %>
        <li><%= error %></li>
      <% end %>
    </ul>
  <% end %>

  <%= f.input :name %>

  <%= f.submit %>
<% end %>